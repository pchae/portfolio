import { createStore } from 'vuex';
import aboutJson from '@/json/about.json';

export default createStore({
  state: {
    about: aboutJson,
  },

  getters: {
  },

  mutations: {
  },

  actions: {
  },

  modules: {
  },
});
